apiVersion: v1
kind: Service
metadata:
  name: jobradarai-web
  namespace: jobradarai
  labels:
    app.kubernetes.io/name: jobradarai-web
    app.kubernetes.io/part-of: jobradarai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: jobradarai-web
    app.kubernetes.io/part-of: jobradarai
  ports:
    - name: http
      port: 80
      targetPort: http
